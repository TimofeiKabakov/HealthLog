import React, { useState } from 'react'

export const Card = (props) => {
    
    const [expanded, setExpanded] = useState(false)

    return (
        <div className='Card'>
            Card
        </div>
    )
}
